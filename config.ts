// variables shared between JS and CSS
interface IConfig {
    BOARD_SIZE: number;
    GA_ANALYTICS_ID: number;
    PORT: number;
}

const config: IConfig = {
    BOARD_SIZE: 3,
    GA_ANALYTICS_ID: 116278935,
    PORT: 3000,
};

export default config;
